# Kynship — Premium Arabic Kitchen & Wardrobe Website

موقع احترافي فاخر لعلامة كينشيب لتصميم المطابخ وغرف الملابس، بلغة عربية كاملة وتخطيط RTL.

## المتطلبات

- Node.js 18+
- npm أو yarn

---

## 🚀 تشغيل المشروع محلياً

```bash
# 1. تثبيت الحزم
npm install

# 2. تشغيل بيئة التطوير
npm run dev
```

افتح [http://localhost:3000](http://localhost:3000) في المتصفح.

---

## ☁️ نشر المشروع على Vercel

### 1. رفع المشروع على GitHub

```bash
git init
git add .
git commit -m "Initial commit: Kynship website"
git remote add origin https://github.com/YOUR_USERNAME/kynship.git
git push -u origin main
```

### 2. ربط GitHub بـ Vercel

1. افتح [vercel.com](https://vercel.com) وسجّل دخولك
2. اضغط **New Project**
3. اختر مستودع `kynship` من GitHub
4. اضغط **Deploy** — Vercel سيكتشف Next.js تلقائياً

### 3. متغيرات البيئة على Vercel

أضف متغيرات البيئة التالية في **Settings → Environment Variables**:

| المتغير | القيمة |
|---------|--------|
| `NEXT_PUBLIC_TINA_CLIENT_ID` | من [tina.io](https://tina.io) |
| `TINA_TOKEN` | من [tina.io](https://tina.io) |

---

## 🎨 تفعيل لوحة الإدارة TinaCMS

### التشغيل المحلي
```bash
npm run dev
```
ثم افتح: `http://localhost:4001/` للوحة الإدارة المحلية.

### الإنتاج
1. أنشئ حساباً على [tina.io](https://tina.io)
2. أنشئ مشروعاً جديداً وربطه بمستودع GitHub
3. انسخ `TINA_PUBLIC_CLIENT_ID` و `TINA_TOKEN`
4. أضفهما في متغيرات بيئة Vercel
5. أعد النشر (Redeploy)
6. افتح `/admin` للوصول للوحة التحكم المرئية

---

## 📁 هيكل المجلدات

```
kynship/
├── pages/
│   ├── index.js          # الصفحة الرئيسية
│   ├── contact.js        # صفحة التواصل
│   ├── admin.js          # إعادة توجيه للإدارة
│   ├── portfolio/
│   │   ├── index.js      # معرض الأعمال
│   │   └── [id].js       # تفاصيل المشروع
│   └── api/
│       ├── projects.js   # API للمشاريع
│       └── contact.js    # API لنموذج التواصل
├── components/           # مكونات React
├── styles/               # ملفات CSS
├── content/
│   └── projects/         # ملفات JSON للمشاريع
├── data/
│   └── reviews.json      # آراء العملاء
├── public/
│   └── images/           # الصور
└── tina/
    └── config.js         # إعداد TinaCMS
```

---

## ✏️ إضافة مشاريع جديدة

أضف ملف JSON جديد في `content/projects/`:

```json
{
  "id": "project-4",
  "title": "اسم المشروع",
  "description": "وصف المشروع",
  "category": "مطابخ",
  "completionDate": "2026-01-01",
  "images": ["/projects/project-4-main.jpg"]
}
```

---

## 🔧 التخصيص

- **الألوان**: عدّل `--gold` في `styles/globals.css`
- **رقم الواتساب**: ابحث عن `wa.me/201000000000` واستبدله
- **الموقع**: عدّل في `components/Footer.jsx`
- **المراجعات**: عدّل `data/reviews.json`

---

## 📄 الترخيص

جميع الحقوق محفوظة © 2026 Kynship
